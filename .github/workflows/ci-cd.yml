name: Binance CI/CD

on:
  push:
    branches: [ "feat/binance" ]
    paths:
      - 'src/binance/**'
      - 'requirements.txt'
      - 'dockerfile'
      - 'deploy_producer.yaml'
      - '.github/workflows/ci-cd.yml'
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: self-hosted
    outputs:
      image_tag: ${{ steps.set_tag.outputs.image_tag }}
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set IMAGE_TAG
        id: set_tag
        run: |
          TIMESTAMP=$(date -u +"%Y%m%d-%H%M%S")
          # Chuyển tên repository về chữ thường để đúng chuẩn Docker image
          IMAGE_REPO=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          IMAGE_TAG="ghcr.io/${IMAGE_REPO}/binance-producer:${TIMESTAMP}"

          echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV
          echo "image_tag=$IMAGE_TAG" >> $GITHUB_OUTPUT
          echo "Using image: $IMAGE_TAG"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ env.IMAGE_TAG }}


  deploy-to-k8s:
    runs-on: self-hosted
    needs: build-and-push # Phải đợi build xong mới deploy
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup kubectl
        id: install-kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: 'latest'

      - name: Load kubeconfig
        run: |
          printf "%s" "${{ secrets.KUBE_CONFIG }}" | base64 -d > kubeconfig
          echo "KUBECONFIG=$PWD/kubeconfig" >> $GITHUB_ENV

      - name: Replace image in deploy_producer.yaml
        run: |
          # Cập nhật tag image mới nhất vào file manifest
          sed -i "s|image: .*|image: ${{ needs.build-and-push.outputs.image_tag }}|g" deploy_producer.yaml

      - name: Apply Kubernetes manifests
        run: |
          $KUBECTL_PATH apply -f deploy_producer.yaml -n kafka
        env:
          KUBECTL_PATH: ${{ steps.install-kubectl.outputs.kubectl-path }}

      - name: Wait for rollout
        run: |
          # Đảm bảo tên deployment trong deploy_producer.yaml là binance-producer
          $KUBECTL_PATH rollout status deployment/binance-producer -n kafka
        env:
          KUBECTL_PATH: ${{ steps.install-kubectl.outputs.kubectl-path }}